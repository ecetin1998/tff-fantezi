import Link from 'next/link'
import { joinProWaitlist } from '@/app/actions'
import { getAuthState } from '@/lib/data'
export default async function Pricing({searchParams}){const auth=await getAuthState(),sp=await searchParams;return <div className="pricing"><div className="section-title centered"><div><span className="eyebrow">FANTEZİ PRO</span><h1>Modelin tamamını aç</h1><p className="muted">Beta sürecinde temel özellikler ücretsiz. Pro talebini şimdiden ölçüyoruz.</p></div></div>{sp?.joined?<div className="alert">Pro listesine eklendin. Açıldığında ilk haber alanlardan olacaksın.</div>:null}<div className="price-grid"><div className="card price-card"><span>ÜCRETSİZ</span><strong>0 TL</strong><p>Oyuncu xFP, maç tahminleri, haftalık model kadrosu ve kendi kadronu kaydetme.</p><Link href="/players" className="secondary">Ücretsiz kullan</Link></div><div className="card price-card featured"><span>FANTEZİ PRO</span><strong>99 TL<small>/ay hedef</small></strong><p>3 MH planlayıcı, gelişmiş kadro optimizasyonu, diferansiyel/risk analizi ve daha derin kişisel öneriler.</p>{auth.plan==='pro'?<div className="cta">Pro aktif</div>:<form action={joinProWaitlist}><button className="cta">Pro'yu istiyorum</button></form>}</div></div></div>}
